{
  "name": "krunalkumar-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for krunalkumar.dpdns.org. No dependencies and no bundler: the pages are hand-written HTML and are served exactly as they appear in this repository. The build step below runs only on Vercel and only rewrites two things — see scripts/build.js for what and why.",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node scripts/build.js --check"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
